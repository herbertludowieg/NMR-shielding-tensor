#include "linearalgebra.h"

LinearAlgebra::LinearAlgebra() {}
LinearAlgebra::~LinearAlgebra() {}

std::vector<std::vector<double> > householder ( 
                       std::vector<std::vector<double> > matrix ) {
  
  double alpha , r;
  std::vector<double> w;
  std::vector<std::vector<double> > identity , b_matrix;
  unsigned int iteration , i , j , m;

  
}
